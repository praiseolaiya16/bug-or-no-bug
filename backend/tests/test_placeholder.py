"""
Placeholder test suite for the Bug-or-No-Bug backend.

TODO: Add tests for:
  - analyzers/static_analysis.py (Bandit/Pylint wrapper output normalization)
  - analyzers/llm_review.py (prompt construction, response parsing, mocked API calls)
  - scoring/metrics.py (precision/recall/F1 computation against known fixtures)
  - app.py routes (health check, /api/analyze, /api/dataset, /api/results)
"""


def test_placeholder():
    """Trivial placeholder so the test suite has at least one passing test.

    TODO: Remove once real tests are added.
    """
    assert True
